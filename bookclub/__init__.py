# Book Club — UI PROTOTYPE ONLY.
#
# This package contains a throwaway, data-free prototype of the Book Club
# feature used to validate UX direction before implementation. It has NO
# models, NO migrations, NO auth, and NO writes. All context is hardcoded in
# prototype_data.py. Do not build on top of it — the real feature will
# introduce proper models/services once the direction is signed off.
